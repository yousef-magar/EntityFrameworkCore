## ✅ **1) إجراءات العلاقات (Referential Actions) بالـ Fluent API**

### 🔹 **سؤال 1:**

أي من الخيارات التالية يجعل حذف سجل "الأب" يؤدي تلقائيًا لحذف جميع الأبناء المرتبطين به؟

A. `DeleteBehavior.Restrict`
B. `DeleteBehavior.NoAction`
C. `DeleteBehavior.Cascade`
D. `DeleteBehavior.SetNull`

**✔ الإجابة الصحيحة: C - DeleteBehavior.Cascade**

**الشرح:**
Cascade معناها "التسلسل"، فلو حذفت الـ **Customer**، يتم حذف كل الـ **Orders** المرتبطين به تلقائيًا. وده بيكون مناسب لما تكون الأبناء ما ينفعش يعيشوا بدون الأب.

---

### 🔹 **سؤال 2:**

لو استخدمت `DeleteBehavior.SetNull`، ما الذي يحدث عند حذف الكيان الأب؟

A. يتم حذف الأبناء أيضًا
B. يتم رفض عملية الحذف
C. يتحوّل المفتاح الخارجي في الأبناء إلى NULL
D. يتم تجاهل الحذف

**✔ الإجابة الصحيحة: C - يتحول FK إلى NULL**

**الشرح:**
`SetNull` معناها: لو الأب اتحذف، الأولاد يفضلوا موجودين، لكن المفتاح الخارجي اللي بيربطهم بالأب بيتحوّل لقيمة NULL.

---

### 🔹 **سؤال 3:**

ما الفرق بين `Restrict` و`NoAction` في EF Core؟

A. `Restrict` يسمح بالحذف و`NoAction` يمنعه
B. الاتنين يمنعوا الحذف لكن التوقيت مختلف
C. `NoAction` يستخدم فقط في PostgreSQL
D. `Restrict` يغيّر قيمة FK، و`NoAction` يحذفه

**✔ الإجابة الصحيحة: B - الاتنين يمنعوا الحذف لكن التوقيت مختلف**

**الشرح:**
الاتنين بيمنعوا حذف الأب لو فيه أبناء، بس الفرق إن **`Restrict`** بيمنع من البداية (قبل تنفيذ أي شيء)، أما **`NoAction`** بيمنع على مستوى قاعدة البيانات (يعني لازم تكون واثق إنك ما خالفتش الشرط).

---

### 🔹 **سؤال 4:**

ما السلوك الافتراضي لـ EF Core عند عدم تحديد `OnDelete`؟ (مع علاقة اختيارية)

A. Cascade
B. SetNull
C. ClientSetNull
D. Restrict

**✔ الإجابة الصحيحة: C - ClientSetNull**

**الشرح:**
الافتراضي هو `ClientSetNull` في العلاقات اللي المفتاح الخارجي فيها قابل لـ NULL. وده يعني إن EF على مستوى **العميل** (Client-Side) هيحط NULL في FK، لكن لازم تنفذ ده يدوي أو تتأكد إنه مش حيخالف قاعدة البيانات.

---

### 🔹 **سؤال 5:**

ما الذي يحدث عند استخدام `DeleteBehavior.ClientSetNull` مع حقل FK غير قابل لـ NULL؟

A. يتم حذف الأبناء
B. يحدث استثناء وقت التنفيذ
C. يتم تجاهل السلوك
D. يتم التحويل لـ Restrict تلقائيًا

**✔ الإجابة الصحيحة: B - يحدث استثناء وقت التنفيذ**

**الشرح:**
`ClientSetNull` بيحاول يخلي الـ FK بـ NULL، فلو النوع بتاعه مش يقبل NULL (زي `int` مش `int?`) يحصل **استثناء وقت التنفيذ** لأنك بتحاول تعيّن NULL في مكان مش يقبله.

---

### تمرين ذهني بسيط قبل نكمل:

**افترض عندك علاقة:**

```csharp
Order --(many)--> Customer
```

لو حد حذف Customer، والـ DeleteBehavior هو `Restrict`، إيه اللي هيحصل؟

✅ مش هيتنفذ الحذف وهيظهر Error إن فيه أبناء مرتبطين بيه.

---

## ✅ **2) تضمين/استبعاد كيان (Include/Exclude Table) في EF Core**

---

### 🔹 **سؤال 6:**

أي من التالي يستخدم لاستبعاد **كيان بالكامل** من التكوين داخل EF Core باستخدام الـ Fluent API؟

A. `[NotMapped]`
B. `modelBuilder.Entity<>().HasNoTable()`
C. `modelBuilder.Ignore<>()`
D. `modelBuilder.Exclude<>()`

**✔ الإجابة الصحيحة: C - modelBuilder.Ignore<>()**

**الشرح:**
`Ignore<T>()` معناه إن الكيان ده مش هيتم اعتباره في التكوين، وبالتالي مش هيتولد له جدول. ودي طريقة Fluent API المكافئة لـ `[NotMapped]`.

---

### 🔹 **سؤال 7:**

ما السطر المناسب لاستبعاد خاصية معينة من التكوين داخل الكيان باستخدام Data Annotation؟

A. `[Ignore]`
B. `[Exclude]`
C. `[NotMapped]`
D. `[SkipProperty]`

**✔ الإجابة الصحيحة: C - \[NotMapped]**

**الشرح:**
`[NotMapped]` معناها إن الخاصية دي مش هتتحفظ في قاعدة البيانات، سواء كانت نتيجة حساب أو شيء مؤقت.

---

### 🔹 **سؤال 8:**

ما الكود الصحيح لتغيير اسم جدول `Student` إلى `tbl_Students` باستخدام Fluent API؟

A. `modelBuilder.Entity<Student>().TableName("tbl_Students");`
B. `modelBuilder.Entity<Student>().ChangeTable("tbl_Students");`
C. `modelBuilder.Entity<Student>().To("tbl_Students");`
D. `modelBuilder.Entity<Student>().ToTable("tbl_Students");`

**✔ الإجابة الصحيحة: D - ToTable("tbl\_Students")**

**الشرح:**
`ToTable()` هي الطريقة المعتمدة لتحديد اسم الجدول البديل. وده مفيد لما تحب تتبع Naming Convention خاص.

---

### 🔹 **سؤال 9:**

أي من التالي يؤدي إلى **عدم توليد جدول** لكيان `ReportViewModel` عند عمل Migration؟

A. جعله `abstract`
B. استخدام `Ignore<ReportViewModel>()` في `OnModelCreating`
C. إضافة خاصية `IsVirtual`
D. تحديد المفتاح الأساسي يدويًا

**✔ الإجابة الصحيحة: B - استخدام Ignore<>()**

**الشرح:**
EF Core بيفترض أي كلاس يمثل كيان حقيقي إلا لو قلت له غير كده صراحة. `Ignore<T>()` بتقول لـ EF "تجاهل الكيان ده تمامًا".

---

## ✅ **3) التحكم في الكيانات بـ Data Annotations**

---

### 🔹 **سؤال 10:**

ما الغرض من `[Required]` في كيان؟

A. التأكد أن الخاصية يتم تحميلها تلقائيًا
B. منع ترك الخاصية Null
C. تفعيل الفهرسة
D. جعل الخاصية مفتاحًا أساسيًا

**✔ الإجابة الصحيحة: B - منع ترك الخاصية Null**

**الشرح:**
`[Required]` معناها إن القيمة دي **لازم** تتملأ، وبالتالي عمود الـ DB هيكون `NOT NULL`.

---

### 🔹 **سؤال 11:**

ما الفرق بين `[MaxLength(100)]` و`[StringLength(100)]`؟

A. لا فرق؛ الاثنين متكافئين
B. `MaxLength` تؤثر على DB فقط و`StringLength` على الواجهة
C. `StringLength` تؤثر على EF فقط
D. `MaxLength` للعدد الكلي و`StringLength` للحد الأدنى

**✔ الإجابة الصحيحة: B - MaxLength تؤثر على DB فقط وStringLength على الواجهة**

**الشرح:**
`MaxLength` تؤثر في EF وDB، أما `StringLength` تؤثر كمان على Data Validation في UI، زي ASP.NET MVC.

---

### 🔹 **سؤال 12:**

ما تأثير استخدام `[Column(TypeName = "decimal(10,2)")]` على خاصية `Price`؟

A. لا يؤثر على شيء
B. يجعل العمود `nvarchar(10)`
C. يحدد دقة وحجم العمود كنطاق عددي عشري
D. يجعله حقل حسابي

**✔ الإجابة الصحيحة: C - يحدد دقة وحجم العمود كنطاق عددي عشري**

**الشرح:**
`decimal(10,2)` تعني: 10 خانات إجمالًا، منهم 2 بعد الفاصلة. وده مهم للأسعار مثلاً.

---

### 🔹 **سؤال 13:**

أي من التالي يسمح بإنشاء فهرس فريد على خاصية باستخدام Data Annotation؟ (بدءًا من EF Core 5)

A. `[Unique]`
B. `[Index(IsUnique = true)]`
C. `[Key]`
D. `[Column(Unique=true)]`

**✔ الإجابة الصحيحة: B - \[Index(IsUnique = true)]**

**الشرح:**
`[Index]` دي Data Annotation مضافة حديثًا، تقدر منها تضيف فهارس عادية أو فريدة مباشرة من الكلاس.

---

## ✅ **4) التحكم في الكيانات بالـ Fluent API**

---

### 🔹 **سؤال 14:**

ما الفائدة الأساسية من استخدام `IEntityTypeConfiguration<T>`؟

A. تسريع الأداء عند التشغيل
B. التحكم في علاقة الجداول فقط
C. تنظيم الإعدادات في ملفات منفصلة
D. منع التعديل على الكيان

**✔ الإجابة الصحيحة: C - تنظيم الإعدادات في ملفات منفصلة**

**الشرح:**
`IEntityTypeConfiguration<T>` بتخليك تحط كل إعدادات الكيان في ملف واحد، وده بيسهّل صيانته وتنظيم المشروع.

---

### 🔹 **سؤال 15:**

في الكود التالي، ماذا يفعل السطر `b.HasIndex(p => p.Name).IsUnique();`؟

```csharp
b.HasIndex(p => p.Name).IsUnique();
```

A. يمنع الحذف إذا الاسم مكرر
B. يحفظ الفهرس داخل كاش
C. ينشئ فهرس فريد على العمود Name
D. يربط Name بكائن خارجي

**✔ الإجابة الصحيحة: C - ينشئ فهرس فريد على العمود Name**

**الشرح:**
`HasIndex().IsUnique()` معناها إن العمود ده مش هيسمح بقيم مكررة، وده زي قيد فريد في DB.

---

## ✅ **5) الأعمدة الافتراضية والمحسوبة (Default & Computed Columns)**

---

### 🔹 **سؤال 16:**

ما تأثير السطر التالي عند إنشاء Migration؟

```csharp
.Property(o => o.CreatedAt).HasDefaultValueSql("GETDATE()");
```

A. يتم تجاهل القيمة الافتراضية
B. يتم إدخال تاريخ اليوم تلقائيًا عند الإدراج
C. يتم احتساب العمر تلقائيًا
D. يتم حذف القيمة إذا لم تُدخل

**✔ الإجابة الصحيحة: B - يتم إدخال تاريخ اليوم تلقائيًا عند الإدراج**

**الشرح:**
`HasDefaultValueSql("GETDATE()")` معناها إن SQL Server هيحط تاريخ اليوم تلقائيًا لو انت ما بعتهوش في INSERT.

---

### 🔹 **سؤال 17:**

ما الفرق بين `Computed` و`Default` في الأعمدة؟

A. Default تملأ عند الإدراج فقط، Computed تحسب دائمًا
B. Default تحسب من أعمدة أخرى، Computed من ثابت
C. Computed تعمل في C# فقط
D. لا يوجد فرق فعليًا

**✔ الإجابة الصحيحة: A - Default تملأ عند الإدراج فقط، Computed تحسب دائمًا**

**الشرح:**
الفرق الجوهري:

* `Default`: قيمة أولية عند الإدراج فقط
* `Computed`: قيمة يتم حسابها دائمًا بناءً على أعمدة تانية، ومش تقدر تعدّلها يدويًا

---

✅ كده خلصنا أول **5 أقسام** بأسئلة تفصيلية وشرح لكل إجابة.

---

## ✅ **6) استخدام الفهارس (Indexing) في EF Core**

---

### 🔹 **سؤال 18:**

لماذا تُستخدم الفهارس (Indexes) في قواعد البيانات وEF Core؟

A. لحذف السجلات بشكل أسرع
B. لتقليل حجم قاعدة البيانات
C. لتحسين سرعة الاستعلامات والفرز
D. لتشفير الأعمدة

**✔ الإجابة الصحيحة: C - لتحسين سرعة الاستعلامات والفرز**

**الشرح:**
الفهرس (Index) بيشتغل زي فهرس الكتاب، بيرفع كفاءة الوصول للبيانات عند البحث أو الفرز أو استخدام JOIN، خاصة في الأعمدة اللي عليها استعلامات متكررة.

---

### 🔹 **سؤال 19:**

ما معنى الفهرس المركب (Composite Index) في EF Core؟

A. فهرس يحتوي على خصائص من أكثر من جدول
B. فهرس يحتوي على أكثر من عمود في نفس الكيان
C. فهرس يتم إنشاؤه يدويًا من SQL فقط
D. فهرس يتم تحديثه يدويًا فقط

**✔ الإجابة الصحيحة: B - فهرس يحتوي على أكثر من عمود في نفس الكيان**

**الشرح:**
Composite Index هو فهرس بيشمل أكتر من عمود زي:

```csharp
.HasIndex(u => new { u.FirstName, u.LastName })
```

مفيد لما تبحث بعدّة أعمدة مع بعض.

---

### 🔹 **سؤال 20:**

ما وظيفة `.HasFilter("[IsDeleted] = 0")` في تعريف الفهرس؟

A. يحدد أعمدة الفهرس
B. يمنع الحذف التلقائي
C. يجعل الفهرس يشمل فقط البيانات غير المحذوفة
D. يحذف الفهرس عند الإقلاع

**✔ الإجابة الصحيحة: C - يجعل الفهرس يشمل فقط البيانات غير المحذوفة**

**الشرح:**
الفلاتر بتقلل حجم الفهرس وتحسّن الأداء عن طريق تجاهل السجلات اللي مش مطلوبة في الفهرس (زي المحذوفة).

---

## ✅ **7) عمليات الفهارس مع الـ Migrations**

---

### 🔹 **سؤال 21:**

ما الذي يحدث عند تغيير خصائص فهرس موجود داخل كيان ثم تنفيذ Migration؟

A. يتم تحديث الفهرس مباشرة
B. يتم حذف الفهرس القديم وإنشاء واحد جديد
C. يتم تجاهل التغيير
D. يظهر خطأ وقت التنفيذ

**✔ الإجابة الصحيحة: B - يتم حذف الفهرس القديم وإنشاء واحد جديد**

**الشرح:**
EF Core مش بيقدر يعدّل الفهرس مباشرة، فلو غيّرت خصائصه (زي اسمه أو الأعمدة)، هيحذف القديم ويعمل فهرس جديد.

---

## ✅ **8) تسلسلات قاعدة البيانات (Database Sequences)**

---

### 🔹 **سؤال 22:**

ما الغرض من استخدام `HasSequence()` في EF Core؟

A. توليد علاقات بين الكيانات
B. إنشاء قاعدة بيانات جديدة
C. إنشاء مولّد أرقام مستقل لتوليد قيم فريدة
D. حذف البيانات تلقائيًا

**✔ الإجابة الصحيحة: C - إنشاء مولّد أرقام مستقل لتوليد قيم فريدة**

**الشرح:**
Sequence = سلسلة أرقام بتزيد تلقائيًا، وبتستخدمها غالبًا للمفاتيح أو أرقام الفواتير.

---

### 🔹 **سؤال 23:**

ما تأثير السطر التالي على خاصية `OrderNumber`؟

```csharp
.HasDefaultValueSql("NEXT VALUE FOR shared.OrderNumbers")
```

A. يجعل القيمة صفر دائمًا
B. يستخدم HiLo
C. يربط الخاصية بتسلسل
D. يحوّلها إلى فهرس

**✔ الإجابة الصحيحة: C - يربط الخاصية بتسلسل**

**الشرح:**
ده معناه إن العمود بياخد القيمة من الـ Sequence تلقائيًا، زي AutoNumber لكن مخصص.

---

## ✅ **9) تحويل الـ Migration إلى SQL (Script)**

---

### 🔹 **سؤال 24:**

ما فائدة أمر `Script-Migration`؟

A. ينفذ الـ Migration
B. يحدد الفهارس فقط
C. يولّد سكربت SQL للـ Migrations
D. يحذف قاعدة البيانات

**✔ الإجابة الصحيحة: C - يولّد سكربت SQL للـ Migrations**

**الشرح:**
`Script-Migration` بتحوّل التغييرات في الكود لسكريبت SQL تقدر تنفذه يدويًا على السيرفر أو في CI/CD Pipeline.

---

## ✅ **10) Reverse Scaffold – توليد الكود من قاعدة البيانات**

---

### 🔹 **سؤال 25:**

ما الهدف من أمر `dotnet ef dbcontext scaffold`؟

A. حذف قاعدة البيانات
B. إنشاء جداول جديدة
C. توليد الكود من قاعدة بيانات موجودة
D. تحويل SQL إلى كود LINQ

**✔ الإجابة الصحيحة: C - توليد الكود من قاعدة بيانات موجودة**

**الشرح:**
Reverse Engineering من SQL → C#، يفيد لو بدأت من قاعدة بيانات جاهزة وعايز تولّد منها الكيانات والـ DbContext تلقائيًا.

---

## ✅ **11) إضافة بيانات مبدئية (Seed Data)**

---

### 🔹 **سؤال 26:**

ما الشرط الضروري لنجاح استخدام `HasData()` في EF Core؟

A. الكيان يجب أن يحتوي على فهارس
B. يجب تفعيل Lazy Loading
C. يجب توفير قيم المفاتيح الأساسية
D. يجب أن تكون خاصية واحدة فقط

**✔ الإجابة الصحيحة: C - يجب توفير قيم المفاتيح الأساسية**

**الشرح:**
EF لازم يعرف المفتاح الأساسي لكل سجل عشان يقدر يحدده في التحديثات التالية (مثلاً لو عملت Seed تاني بنفس المفتاح).

---

## ✅ **12) EF Core و LINQ**

---

### 🔹 **سؤال 27:**

ما الغرض من استخدام `AsNoTracking()` مع استعلام LINQ في EF Core؟

A. تحسين الأداء عند قراءة البيانات
B. منع تحميل البيانات
C. حذف الكيانات من الذاكرة
D. تخزين البيانات مؤقتًا

**✔ الإجابة الصحيحة: A - تحسين الأداء عند قراءة البيانات**

**الشرح:**
`AsNoTracking()` بيخلي EF يجيب البيانات من DB ويخلص، بدون ما يتتبعها في الذاكرة، فبتكون أسرع في القراءة.

---

## ✅ **13 + 14) التدقيق (Auditing) والتحديث العام**

---

### 🔹 **سؤال 28:**

ما الذي يميز الكيانات التي تطبق واجهة `IAuditable`؟

A. لا تُخزن في قاعدة البيانات
B. يتم تجاهلها عند الحذف
C. يتم ملء حقول `CreatedAt`, `CreatedBy` تلقائيًا
D. تستخدم فقط في الجداول المحذوفة

**✔ الإجابة الصحيحة: C - يتم ملء الحقول تلقائيًا**

**الشرح:**
الفكرة إنك تخلي EF يضبط التواريخ والمستخدم تلقائيًا عند SaveChanges، خاصة عند الإضافة أو التعديل.

---

## ✅ **15) الحذف العام (Soft Delete)**

---

### 🔹 **سؤال 29:**

ما فائدة `HasQueryFilter(u => !u.IsDeleted)` في EF Core؟

A. حذف السجلات القديمة تلقائيًا
B. منع الحذف من الكود
C. جعل جميع الاستعلامات تتجاهل السجلات المحذوفة منطقيًا
D. تفعيل خاصية الفهرسة

**✔ الإجابة الصحيحة: C - جعل جميع الاستعلامات تتجاهل السجلات المحذوفة منطقيًا**

**الشرح:**
`Soft Delete` = بدل ما تمسح، تعلّم السجل بـ `IsDeleted = true`، وEF يفلترهم تلقائيًا باستخدام HasQueryFilter.

---

## ✅ **16) التتبع مقابل عدم التتبع**

---

### 🔹 **سؤال 30:**

ما الذي يحدث لو عدّلت كيان تم جلبه بـ `AsNoTracking()` بدون `Attach()` أو `Update()`؟

A. يتم حفظ التعديلات تلقائيًا
B. لا يتم حفظ أي تعديل
C. يتم إنشاء كيان جديد
D. يحدث استثناء

**✔ الإجابة الصحيحة: B - لا يتم حفظ أي تعديل**

**الشرح:**
`AsNoTracking` معناها EF مش هيراقب التعديلات، فلازم `Attach()` أو `Update()` قبل ما تحاول تحفظ التغييرات.

---

## ✅ **17, 18, 19) التحميل (Eager/Explicit/Lazy)**

---

### 🔹 **سؤال 31:**

ما الفرق بين `Include()` و`ThenInclude()` في EF Core؟

A. Include للعلاقات المفردة، ThenInclude للمتعددة
B. Include لأول مستوى من العلاقات، ThenInclude للمستوى التالي
C. ThenInclude يستخدم مع LINQ فقط
D. لا يوجد فرق

**✔ الإجابة الصحيحة: B - ThenInclude للمستوى التالي**

**الشرح:**
`Include()` بيحمل علاقة، و\`Then


Include()\` بيكمل تحميل علاقات داخل العلاقة دي (nested).

---

## ✅ **20) الأداء وتحسين الاستعلامات**

---

### 🔹 **سؤال 32:**

ما الفائدة من `ToListAsync()` بدلاً من `ToList()` في EF Core؟

A. لا فرق
B. `ToList()` تستخدم في الـ Web فقط
C. `ToListAsync()` لا يحجز الـ Thread
D. `ToListAsync()` تحذف البيانات بعد القراءة

**✔ الإجابة الصحيحة: C - ToListAsync() لا يحجز الـ Thread**

**الشرح:**
`ToListAsync()` بتنفذ الاستعلام بشكل غير متزامن (Asynchronous)، وده مفيد في تطبيقات الويب لتحسين الأداء والاستجابة.

---

## ✅ مبروك 🎉

خلصنا كل الأقسام الـ 20 بأسئلة Multiple Choice وشرح عملي وسلس لكل إجابة.

---







